---
title: Quickstart
description: Build a runtime bundle, overlay it on any Linux task image, and call a remote function with RuntimeClient.remote.
---

This quickstart walks the shortest end-to-end Agentix flow:

1. Declare deps in a normal Python project.
2. `agentix build` packages the project + every transitive dep into a
   self-contained bundle — no `FROM`, no `pip install` at run
   time, just a Nix closure under `/nix/store/...`.
3. Deploy a sandbox: the bundle is bind-mounted onto a task base image
   at `/nix`. Different task images, same runtime.
4. Call a remote function with `RuntimeClient.remote(fn, …)`.

## Prerequisites

- Python 3.11 or newer
- Docker or Podman for the build executor and local sandbox backend
- `uv` for dependency locking (`uv.lock` is mandatory; the build
  container runs `uv sync` against it)

## One project, two images

The two-image model is the heart of Agentix:

- **`bundle`** — the bundle from `agentix build`. Generic and
  reusable. Holds Python, agentixx, every plugin, your code, all
  transitive deps under `/nix/store/...` with libc vendored.
- **`image`** — the task-specific base the workload runs in (a
  SWE-bench task image, a customer environment, plain Ubuntu, …).

`DockerDeployment` mounts the runtime's `/nix` into the task container
at sandbox-create time. The Python interpreter, the runtime server, your
code, and the plugin binaries (bash, coreutils, …) all live under
`/nix/runtime/bin/` and resolve regardless of the task image's
distribution.

## Layout

```text
hello-agentix/
├── pyproject.toml
├── uv.lock                       # run `uv lock`
├── run.py                        # host orchestrator
└── src/
    └── hello_agentix/
        └── __init__.py           # sandbox-side module (placeholder here)
```

<Tabs>
  <Tab title="pyproject.toml">
    ```toml
    [project]
    name = "hello-agentix"
    version = "0.1.0"
    requires-python = ">=3.11"
    dependencies = [
        "agentixx>=0.1.5",
        "agentix-runtime-basic>=0.1.4",      # bash + files namespaces
        "agentix-deployment-docker>=0.1.4",  # docker/podman backend
    ]

    [build-system]
    requires = ["hatchling"]
    build-backend = "hatchling.build"

    [tool.hatch.build.targets.wheel]
    packages = ["src/hello_agentix"]
    ```
  </Tab>
  <Tab title="src/hello_agentix/__init__.py">
    ```python
    # Pure placeholder for this quickstart — bash.run from
    # agentix-runtime-basic does all the work.
    #
    # Add your own functions when you outgrow bash:
    #
    #     async def greet(name: str) -> str:
    #         return f"hello {name} from inside the sandbox"
    ```
  </Tab>
  <Tab title="uv.lock">
    Run once and commit:

    ```bash
    uv lock
    ```

    `agentix build` refuses to run without it because the builder runs
    `uv sync` against the project lock.
  </Tab>
</Tabs>

## Install + build

```bash
cd hello-agentix
uv sync
uv run agentix build . --name hello-agentix --output dist/hello-agentix.bundle.tar
uv run agentix deploy docker dist/hello-agentix.bundle.tar
# → bundle -> /home/me/.cache/agentix/bundles/sha256-...
```

`agentix build` does, in order:

1. Stages your project + every plugin's `default.nix` (each plugin
   ships its system deps next to its Python module).
2. Runs Nix for the interpreter/system closures and `uv sync` for the
   Python dependency closure inside the build container.
3. Writes a portable bundle tar containing `manifest.json + nix/`.

`agentix deploy docker|podman` unpacks that tar into a content-addressed
host cache. Docker-compatible sandbox runs bind-mount the cached `nix/`
tree at `/nix`. No runtime `pip install`.

## Call it

```python title="run.py"
import asyncio

from agentix import RuntimeClient, SandboxConfig, session
from agentix.bash import run
from agentix.deployment.docker import DockerDeployment


async def main() -> None:
    deployment = DockerDeployment()
    config = SandboxConfig(
        # Task base image — workload environment.
        image="python:3.13-slim",
        # Cache path printed by `agentix deploy docker|podman`.
        bundle="/home/me/.cache/agentix/bundles/sha256-...",
    )
    async with session(deployment, config) as sandbox:
        print(f"sandbox up at {sandbox.runtime_url}")
        async with RuntimeClient(sandbox.runtime_url) as client:
            result = await client.remote(run, command="echo hello from $(uname -a)")
            print(f"exit={result.exit_code} stdout={result.stdout!r}")


asyncio.run(main())
```

```bash
uv run python run.py
```

Expected output:

```text
sandbox up at http://localhost:33803
exit=0 stdout='hello from Linux <kernel> ... GNU/Linux\n'
```

## How the overlay works

Under the hood, `DockerDeployment.create` does:

```bash
# Per sandbox — task image plus the cached runtime /nix.
docker run -d --name <sid> -p 127.0.0.1:<port>:<port> \
    -e AGENTIX_BIND_PORT=<port> \
    --mount type=bind,source=/home/me/.cache/agentix/bundles/sha256-.../nix,target=/nix,readonly \
    --entrypoint /nix/runtime/bootstrap.sh \
    python:3.13-slim
```

The task container sees `/nix/store/...` and `/nix/runtime/bin/...` at
their canonical paths. Python's RPATH (baked at Nix build time) finds
its libs under `/nix/store/...` because that's exactly where the
mounted cache puts them.

## Next

<CardGroup cols={2}>
  <Card title="Cookbook: hello-agentix" icon="book" href="https://github.com/Agentiix/agentix-cookbook/tree/main/examples/hello-agentix">
    The complete project files used here.
  </Card>
  <Card title="Cookbook: eval-cc-swe" icon="flask" href="https://github.com/Agentiix/agentix-cookbook/tree/main/examples/eval-cc-swe">
    Larger example: Claude Code on SWE-bench Verified.
  </Card>
  <Card title="Architecture" icon="sitemap" href="/reference/architecture">
    Bundles, workers, and transports.
  </Card>
  <Card title="CLI Reference" icon="terminal" href="/reference/cli">
    Every `agentix build` option.
  </Card>
</CardGroup>
